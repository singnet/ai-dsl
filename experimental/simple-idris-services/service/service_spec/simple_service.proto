syntax = "proto3";

package simple_service;

message Arguments {
    uint32 argument = 1;
}

message Result {
    uint32 value = 1;
}

service Simple {
    rpc incrementer(Arguments) returns (Result) {}
    rpc twicer(Arguments) returns (Result) {}
    rpc halfer(Arguments) returns (Result) {}
}
